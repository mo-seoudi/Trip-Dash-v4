import React,{useEffect,useState}from"react";import{useNavigate}from"react-router-dom";import{FiDatabase,FiGitBranch,FiGrid,FiShield,FiUsers}from"react-icons/fi";import{toast}from"react-toastify";import api from"@/services/apiClient";
const label=v=>String(v||"—").replaceAll("_"," ").toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());function Card({title,value,icon:Icon}){return <div className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between"><div><p className="text-sm text-slate-500">{title}</p><p className="mt-2 text-3xl font-bold">{value}</p></div><span className="h-fit rounded-xl bg-violet-50 p-2.5 text-violet-600"><Icon/></span></div></div>}
export default function DelegatedControlCenter(){const navigate=useNavigate(),[context,setContext]=useState(null),[data,setData]=useState(null),[tenantId,setTenantId]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{try{const c=(await api.get("/delegated-admin/context")).data;if(c.mode==="platform")return navigate("/admin/platform",{replace:true});setContext(c);setTenantId(c.default_tenant_id||"")}catch(e){toast.error(e?.response?.data?.message||"Could not load administration scope");setLoading(false)}})()},[navigate]);useEffect(()=>{if(!tenantId)return;(async()=>{setLoading(true);try{setData((await api.get(`/delegated-admin/tenants/${tenantId}`)).data)}catch(e){toast.error(e?.response?.data?.message||"Could not load tenant administration")}finally{setLoading(false)}})()},[tenantId]);if(loading&&!data)return <div className="p-6 text-slate-500">Loading Control Center…</div>;if(!data)return null;const m=data.metrics;return <div className="space-y-6 p-4 md:p-6"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-violet-600">Tenant Administration</p><h1 className="mt-1 text-3xl font-bold">Control Center</h1><p className="mt-2 text-sm text-slate-500">Your administration view is restricted to organizations and people within your assigned tenant scope.</p></div>{context?.tenant_scopes?.length>1&&<select value={tenantId} onChange={e=>setTenantId(e.target.value)} className="rounded-lg border bg-white px-3 py-2 text-sm">{context.tenant_scopes.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select>}</div><section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><FiShield className="text-violet-600"/><span className="text-xs font-bold uppercase text-slate-400">Managed tenant</span></div><h2 className="mt-2 text-2xl font-bold">{data.tenant.name}</h2><p className="text-sm text-slate-500">{data.tenant.slug} · {label(data.tenant.subscription_mode)} · {data.tenant.plan_key||"No plan assigned"}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">{label(data.tenant.status)}</span></div></section><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Card title="Organizations" value={m.organizations} icon={FiGrid}/><Card title="People" value={m.users} icon={FiUsers}/><Card title="Relationships" value={m.relationships} icon={FiGitBranch}/><Card title="Active data sources" value={m.active_data_sources} icon={FiDatabase}/></div><div className="grid gap-4 xl:grid-cols-2"><section className="rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-bold">Organizations</h2><p className="text-sm text-slate-500">Only organizations covered by this tenant are visible here.</p></div><div className="grid gap-3 p-5 sm:grid-cols-2">{data.organizations.map(o=><div key={o.id} className="rounded-xl border p-4"><div className="flex gap-3">{o.abbreviation&&<span className="h-fit rounded-lg bg-violet-50 px-2 py-1 text-xs font-bold text-violet-700">{o.abbreviation}</span>}<div><div className="font-bold">{o.display_name}</div><div className="text-sm text-slate-400">{o.full_name}</div><div className="mt-2 text-xs font-semibold uppercase text-slate-400">{label(o.type)}</div></div></div></div>)}</div></section><section className="rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-bold">People & roles</h2><p className="text-sm text-slate-500">Tenant-wide and organization-scoped access inside your environment.</p></div><div className="space-y-2 p-5">{data.assignments.map(a=><div key={a.id} className="rounded-xl bg-slate-50 p-3"><div className="flex justify-between gap-3"><div><div className="text-sm font-semibold">{a.user.display_name||a.user.email}</div><div className="text-xs text-slate-400">{a.user.email}</div></div><span className="h-fit rounded-full bg-violet-50 px-2 py-1 text-xs font-semibold text-violet-700">{a.role.name}</span></div>{a.organization&&<div className="mt-2 text-xs text-slate-500">{a.organization.display_name}</div>}</div>)}{!data.assignments.length&&<p className="text-sm text-slate-400">No scoped role assignments.</p>}</div></section></div><div className="grid gap-4 xl:grid-cols-2"><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold">Relationships</h2><div className="mt-4 space-y-2">{data.relationships.map(r=><div key={r.id} className="rounded-xl bg-slate-50 p-3 text-sm"><b>{r.from.display_name}</b><span className="mx-2 text-slate-400">→ {label(r.type)} →</span><b>{r.to.display_name}</b></div>)}</div></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold">Infrastructure visibility</h2><div className="mt-4 space-y-2">{data.data_sources.map(s=><div key={s.id} className="flex justify-between rounded-xl bg-slate-50 p-3"><div><div className="text-sm font-semibold">{s.name}</div><div className="text-xs text-slate-400">{label(s.mode)} · {s.provider_label}</div></div><span className={`text-xs font-semibold ${s.is_active?"text-emerald-600":"text-slate-400"}`}>{s.is_active?"Active":"Inactive"}</span></div>)}{!data.data_sources.length&&<p className="text-sm text-slate-400">No data sources configured.</p>}</div></section></div></div>}
