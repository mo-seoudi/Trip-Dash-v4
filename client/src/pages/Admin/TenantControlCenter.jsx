import React,{useEffect,useState}from"react";import{useNavigate,useParams}from"react-router-dom";import{FiArrowLeft,FiDatabase,FiGitBranch,FiGrid,FiSettings,FiUsers}from"react-icons/fi";import{toast}from"react-toastify";import api from"@/services/apiClient";const label=v=>String(v||"—").replaceAll("_"," ").toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());const date=v=>v?new Intl.DateTimeFormat(undefined,{day:"2-digit",month:"short",year:"numeric"}).format(new Date(v)):"Not set";function Card({title,value,icon:Icon}){return <div className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between"><div><p className="text-sm text-slate-500">{title}</p><p className="mt-2 text-3xl font-bold text-slate-900">{value}</p></div><span className="h-fit rounded-xl bg-violet-50 p-2.5 text-violet-600"><Icon/></span></div></div>}
export default function TenantControlCenter(){const{tenantId}=useParams(),navigate=useNavigate(),[data,setData]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{try{setData((await api.get(`/platform-control/tenants/${encodeURIComponent(tenantId)}`)).data)}catch(e){toast.error(e?.response?.data?.message||"Could not load tenant")}finally{setLoading(false)}})()},[tenantId]);if(loading)return <div className="p-6 text-slate-500">Loading tenant…</div>;if(!data)return null;const{tenant,metrics:m}=data;return <div className="space-y-6 p-4 md:p-6"><button onClick={()=>navigate("/admin")} className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-violet-600"><FiArrowLeft/>Platform Control Center</button><div className="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><div className="flex items-center gap-2"><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">{label(tenant.status)}</span><span className="text-xs font-semibold uppercase tracking-wider text-slate-400">Tenant</span></div><h1 className="mt-2 text-3xl font-bold text-slate-900">{tenant.name}</h1><p className="mt-1 text-sm text-slate-500">{tenant.slug} · {tenant.billing_contact_email||"No billing contact"}</p></div><button onClick={()=>navigate(`/admin/access?tenant=${encodeURIComponent(tenant.id)}`)} className="inline-flex items-center gap-2 rounded-lg bg-violet-600 px-4 py-2 text-sm font-semibold text-white"><FiSettings/>Manage tenant</button></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5"><Card title="Organizations" value={m.organizations} icon={FiGrid}/><Card title="Users" value={m.users} icon={FiUsers}/><Card title="Relationships" value={m.relationships} icon={FiGitBranch}/><Card title="Active memberships" value={m.active_memberships} icon={FiUsers}/><Card title="Active data sources" value={m.active_data_sources} icon={FiDatabase}/></div><div className="grid gap-4 xl:grid-cols-3"><section className="rounded-2xl border bg-white p-5 shadow-sm xl:col-span-2"><h2 className="text-lg font-bold text-slate-900">Subscription & account</h2><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-xs font-semibold uppercase text-slate-400">Mode</p><p className="mt-1 font-medium">{label(tenant.subscription_mode)}</p></div><div><p className="text-xs font-semibold uppercase text-slate-400">Plan</p><p className="mt-1 font-medium">{tenant.plan_key||"Not assigned"}</p></div><div><p className="text-xs font-semibold uppercase text-slate-400">Start</p><p className="mt-1 font-medium">{date(tenant.subscription_start)}</p></div><div><p className="text-xs font-semibold uppercase text-slate-400">End / renewal</p><p className="mt-1 font-medium">{date(tenant.subscription_end)}</p></div></div></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-bold text-slate-900">Infrastructure</h2><p className="mt-2 text-sm text-slate-500">{m.active_data_sources} of {m.data_sources} configured data sources active.</p><div className="mt-4 space-y-2">{data.data_sources.slice(0,3).map(s=><div key={s.id} className="rounded-xl bg-slate-50 p-3"><div className="font-semibold text-slate-700">{s.name}</div><div className="text-xs text-slate-400">{label(s.mode)} · {s.provider_label||s.engine}</div></div>)}{!data.data_sources.length&&<p className="text-sm text-slate-400">No data sources configured.</p>}</div></section></div><section className="rounded-2xl border bg-white shadow-sm"><div className="border-b p-5"><h2 className="text-lg font-bold text-slate-900">Organizations</h2><p className="mt-1 text-sm text-slate-500">Organizations covered by this tenant subscription. Open one to inspect its people, relationships, access and infrastructure.</p></div><div className="grid gap-3 p-5 md:grid-cols-2 xl:grid-cols-3">{data.organizations.map(o=><button key={o.id} onClick={()=>navigate(`/admin/organizations/${o.id}`)} className="rounded-xl border border-slate-200 p-4 text-left transition hover:border-violet-300 hover:bg-violet-50/40"><div className="flex items-start gap-3">{o.abbreviation&&<span className="rounded-lg bg-violet-50 px-2 py-1 text-xs font-bold text-violet-700">{o.abbreviation}</span>}<div><div className="font-bold text-slate-900">{o.display_name}</div><div className="text-sm text-slate-400">{o.full_name}</div><div className="mt-2 text-xs font-semibold uppercase tracking-wide text-slate-400">{label(o.type)}</div></div></div></button>)}{!data.organizations.length&&<p className="text-sm text-slate-400">No organizations covered.</p>}</div></section><div className="grid gap-4 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-900">Organization relationships</h2><div className="mt-4 space-y-3">{data.relationships.slice(0,6).map(r=><div key={r.id} className="rounded-xl bg-slate-50 p-3 text-sm"><span className="font-semibold">{r.from?.display_name}</span><span className="mx-2 text-slate-400">→ {label(r.type)} →</span><span className="font-semibold">{r.to?.display_name}</span></div>)}{!data.relationships.length&&<p className="text-sm text-slate-400">No active relationships.</p>}</div></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-900">People & access</h2><p className="mt-1 text-sm text-slate-500">{m.users} people currently appear in this tenant environment.</p><div className="mt-4 space-y-2">{data.users.slice(0,6).map(u=><div key={u.id} className="flex items-center justify-between rounded-xl bg-slate-50 p-3"><div><div className="text-sm font-semibold text-slate-800">{u.display_name||u.email}</div><div className="text-xs text-slate-400">{u.email}</div></div><span className="text-xs font-semibold text-slate-500">{label(u.status)}</span></div>)}</div></section></div></div>}
