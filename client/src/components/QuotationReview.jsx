import React,{useEffect,useMemo,useState}from"react";
import{approveWorkspaceQuotation,getWorkspaceQuotation}from"../services/tripService";
import{useAuth}from"../context/AuthContext";import{toast}from"react-toastify";
const hasPermission=(access,permission)=>Array.isArray(access?.permissions)&&access.permissions.includes(permission);
const QuotationReview=({trip,onUpdated})=>{const{activeWorkspace,access}=useAuth(),schoolId=activeWorkspace?.schoolId;const[quotation,setQuotation]=useState(null),[loading,setLoading]=useState(true),[acting,setActing]=useState(false);const canApprove=hasPermission(access,"trip.approve_quote")&&trip?.status==="Quotation Submitted";
useEffect(()=>{let live=true;(async()=>{if(!schoolId||!trip?.id)return setLoading(false);try{setLoading(true);const q=await getWorkspaceQuotation(schoolId,trip.id);if(live)setQuotation(q);}catch(e){if(live)toast.error(e?.response?.data?.message||"Unable to load quotation.");}finally{if(live)setLoading(false);}})();return()=>{live=false};},[schoolId,trip?.id,trip?.status]);
const total=useMemo(()=>quotation?.lines?.reduce((sum,l)=>sum+(Number(l.price)||0),0)||0,[quotation]);const currencies=[...new Set((quotation?.lines||[]).map(l=>l.currency||"AED"))];const approve=async()=>{try{setActing(true);await approveWorkspaceQuotation(schoolId,trip.id);toast.success("Quotation approved.");onUpdated?.({...trip,status:"Approved"});}catch(e){toast.error(e?.response?.data?.message||"Unable to approve quotation.");}finally{setActing(false);}};
if(loading)return <div className="rounded-lg border bg-white p-4 text-sm text-gray-500">Loading quotation…</div>;if(!quotation)return null;return <section className="rounded-lg border bg-white p-4 space-y-3"><div className="flex items-start justify-between gap-4"><div><h3 className="font-semibold text-gray-900">Quotation</h3><p className="text-sm text-gray-500">Version {quotation.version} · {quotation.status}</p></div><div className="text-right"><p className="text-xs uppercase tracking-wide text-gray-500">Quoted total</p><p className="text-lg font-semibold">{currencies.length===1?currencies[0]:""} {total.toFixed(2)}</p></div></div><div className="divide-y rounded border">{quotation.lines?.map(line=><div key={line.id} className="flex justify-between gap-4 p-3 text-sm"><div><p className="font-medium">Bus {line.sequence}: {line.busType}</p><p className="text-gray-500">{line.seatCapacity?`${line.seatCapacity} seats`:"Capacity not specified"}</p></div><p className="font-medium whitespace-nowrap">{line.currency||"AED"} {Number(line.price).toFixed(2)}</p></div>)}</div>{trip?.status==="Quotation Submitted"&&!canApprove&&<p className="rounded bg-amber-50 p-3 text-sm text-amber-800">This quotation is awaiting approval from an authorized school approver.</p>}{canApprove&&<button type="button" disabled={acting} onClick={approve} className="w-full rounded bg-blue-600 px-4 py-2 text-white disabled:opacity-60">{acting?"Approving…":"Approve Quotation"}</button>}{trip?.status==="Approved"&&<p className="rounded bg-green-50 p-3 text-sm text-green-800">Quotation approved. The Bus Operator can now complete the operational details and confirm the trip.</p>}</section>};export default QuotationReview;
